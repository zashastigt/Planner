services:
  planner:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      network: host
    ports:
      - "8080:8080"
  planner-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      network: host